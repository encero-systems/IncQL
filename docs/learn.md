---
search:
  exclude: true
---

<!--
The top navigation labels this section "Learn" while the pages live under
`language/`. Readers guess `/learn/` from the label, so this stub answers that
guess instead of returning a 404. It is intentionally excluded from search so
only the real overview is indexed.
-->

<meta http-equiv="refresh" content="0; url=../language/">

# Learn IncQL

This page has moved to [Learn IncQL](language/README.md). You should be redirected automatically.
